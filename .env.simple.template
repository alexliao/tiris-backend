# Simple Environment Configuration for Tiris Backend
# Minimal setup - only essential variables needed

# ============================================================================
# Basic Application Settings
# ============================================================================
APP_PORT=8080
HTTP_PORT=80
HTTPS_PORT=443
LOG_LEVEL=info

# ============================================================================
# Database Settings (Required)
# ============================================================================
DB_NAME=tiris
DB_USER=tiris_user
DB_PASSWORD=change_me_in_production
DB_PORT=5432

# ============================================================================
# Security Settings (Required)
# ============================================================================
# Generate with: openssl rand -base64 32
JWT_SECRET=change_me_very_strong_jwt_secret_32_chars_minimum
REFRESH_SECRET=change_me_very_strong_refresh_secret_32_chars_minimum

# ============================================================================
# OAuth Settings (Optional - for Google login)
# ============================================================================
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ============================================================================
# CORS Settings
# ============================================================================
# For HTTP deployment (default)
CORS_ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080
# For HTTPS deployment (updated automatically when using SSL)
# CORS_ALLOWED_ORIGINS=https://yourdomain.com

# ============================================================================
# Quick Setup Instructions:
# ============================================================================
# 1. Copy this file: cp .env.simple.template .env.simple
# 2. Edit the file and change the passwords and secrets
# 3. Generate strong secrets with: openssl rand -base64 32
#
# HTTP Deployment (default):
# 4. Deploy with: docker-compose -f docker-compose.simple.yml --env-file .env.simple up -d
# 5. Access at: http://localhost:8080
#
# HTTPS Deployment (with Let's Encrypt):
# 4. Setup SSL: ./scripts/setup-letsencrypt.sh --domain yourdomain.com --email your@email.com
# 5. Deploy with: docker-compose -f docker-compose.simple.yml --env-file .env.simple --profile ssl up -d
# 6. Access at: https://yourdomain.com (HTTP will redirect to HTTPS)
#
# Or use the interactive deployment:
# ./scripts/quick-deploy.sh (choose option 4 for HTTPS)
# ============================================================================